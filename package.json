{
  "name": "aerovault",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "backend:setup-accounts": "npm run setup-accounts --workspace backend",
    "backend:seed-roles": "npm run seed-roles --workspace backend"
  }
}
